version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - database
    command: ["./wait-for-postgres.sh", "database", "python", "app.py"]
    ports:
      - "${SERVER_PORT}:${SERVER_PORT}"
    env_file: .env

  database:
    image: postgres:14.2-alpine
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: ${POSTGRES_DB_NAME}
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "${POSTGRES_PORT}:5432"

volumes:
  db_data:
